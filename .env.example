# Copy this file to .env and fill in your own key for local testing
# (netlify dev / vercel dev read this automatically). Never commit the
# real .env file — it's already excluded in .gitignore.
#
# Only needed if you want the "This site's AI (Groq)" option to work.
# Get a free key at https://console.groq.com
GROQ_API_KEY=
