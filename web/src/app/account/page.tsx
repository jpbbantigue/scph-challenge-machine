import AccountShell from "@/components/account/AccountShell";

export const metadata = {
  title: "Account — Prompt Royale",
  robots: { index: false, follow: false }
};

export default function AccountPage() {
  return <AccountShell />;
}
